const identity = {
		firstName: 'jubilant',
		lastName: 'ihensekhien'
		};


const getFullName = function(name){

		return (`${name.firstName} ${name.lastName}`);
	
	}
console.log(getFullName(identity))